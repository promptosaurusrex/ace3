// Not obfuscated, but still real JS — used to verify the analyzer
// happily processes a clean file and emits events.
const target = "https://example.com/plain-target";
window.location.href = target;
